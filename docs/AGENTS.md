# Codex documentation guidance

Read `docs/CLAUDE.md` in full before changing anything under `docs/` and treat it as the
canonical documentation-site guidance. Read the repository-root `AGENTS.md` and its linked
`.claude/CLAUDE.md` first; this file adds to, rather than replaces, those instructions.

Interpret references to the project `CLAUDE.md` as `.claude/CLAUDE.md`. The `compare` skill is
available to Codex through `.agents/skills/compare/SKILL.md`.
