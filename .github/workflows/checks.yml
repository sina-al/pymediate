name: Checks

on:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "tests/**"
      - "scripts/**"
      - "pyproject.toml"
      - "uv.lock"
      - "tasks.toml"
      - "ruff.toml"
      - "mypy.ini"
      - ".github/workflows/**"
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: "checks-${{ github.workflow }}-${{ github.ref }}"
  cancel-in-progress: true

# Least privilege: no job here needs more than repo read.
permissions:
  contents: read

jobs:
  changes:
    name: Detect Changed Files
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    outputs:
      code: ${{ steps.filter.outputs.code }}
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: Detect changed paths
        id: filter
        uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4.0.2
        with:
          filters: |
            code:
              - "src/**"
              - "tests/**"
              - "scripts/**"
              - "pyproject.toml"
              - "uv.lock"
              - "tasks.toml"
              - "ruff.toml"
              - "mypy.ini"
              - ".github/workflows/**"

  check:
    name: Run Checks
    runs-on: ubuntu-latest
    needs: [changes]
    if: |
      always() &&
      (github.event_name != 'pull_request' || needs.changes.outputs.code == 'true')

    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.13"

      - name: Setup uv
        uses: astral-sh/setup-uv@d31148d669074a8d0a63714ba94f3201e7020bc3 # v8.3.0

      - name: Sync dependencies
        run: |
          uv sync --only-dev

      - name: Format
        run: |
          uv run poe format:check

      - name: Lint
        run: |
          uv run poe lint --output-format=github

      - name: Type check
        run: |
          uv run poe type

      - name: Lint workflows (zizmor)
        # GH_TOKEN enables zizmor's online audits (e.g. checking action pins against
        # published releases); --format github annotates findings inline on the PR.
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          uv run poe actions:lint --format github

  checks-summary:
    name: Checks
    runs-on: ubuntu-latest
    needs: [changes, check]
    if: always()
    steps:
      - name: Check result
        run: |
          if [[ "${{ needs.check.result }}" == "failure" ]]; then
            echo "Checks failed"
            exit 1
          fi
          echo "Checks passed (or were skipped: no relevant changes)"
