[project]
name = "shop-postgres"
version = "0.1.0"
description = "The shop composed on PostgreSQL persistence, with a Redis-fed export worker"
requires-python = ">=3.12"
dependencies = [
    "shop-core",
    "shop-delivery",
    "shop-adapter-postgres",
    # The stub services (gateway, mailer, storage, audit) live with the memory adapter;
    # only persistence changes between variants.
    "shop-adapter-memory",
]

[project.scripts]
shop-web = "shop_app_postgres.web:main"
shop-worker = "shop_app_postgres.worker:main"
shop = "shop_app_postgres.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shop_app_postgres"]

[tool.uv.sources]
shop-core = { workspace = true }
shop-delivery = { workspace = true }
shop-adapter-postgres = { workspace = true }
shop-adapter-memory = { workspace = true }
