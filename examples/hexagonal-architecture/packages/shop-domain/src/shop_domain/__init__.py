"""The shop's entities: what the business talks about, in plain dataclasses.

This package depends on nothing — not pymediate, not the ports, certainly not a
database. Everything else in the workspace can import it; it imports only the
standard library.
"""
