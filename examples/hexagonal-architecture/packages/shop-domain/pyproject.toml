[project]
name = "shop-domain"
version = "0.1.0"
description = "The shop's entities — plain dataclasses, no dependencies at all"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shop_domain"]
