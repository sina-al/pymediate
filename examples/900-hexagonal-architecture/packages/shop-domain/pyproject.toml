[project]
name = "shop-domain"
version = "0.1.0"
description = "Framework-free business model for the example shop"
readme = "README.md"
requires-python = ">=3.12"

[build-system]
requires = ["uv_build>=0.11.26,<0.12"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "shop.domain"
