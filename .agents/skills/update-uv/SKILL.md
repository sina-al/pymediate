---
name: update-uv
description: Bump the pinned uv version in this repo (pyproject.toml's [tool.uv] required-version) and update the local uv install to match. Use when asked to update or upgrade uv, or when `poe update-uv:check` reports the pin is behind latest.
---

# Update uv

Read `.claude/skills/update-uv/SKILL.md` in full and follow it as the canonical workflow. Treat
`/update-uv` as this skill's name; no other provider-specific changes apply.
