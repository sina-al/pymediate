---
name: release
description: Cut a versioned release of pymediate — dispatch the Prepare Release workflow, review and merge the release PR into stable, then approve the PyPI environment gate. Use when asked to release, publish, or cut or ship a new version.
---

# Release

Read `.claude/skills/release/SKILL.md` in full and follow it as the canonical workflow. Treat
`/release` as this skill's name. Continue to require explicit maintainer confirmation at every
approval or irreversible boundary described by the canonical workflow.
